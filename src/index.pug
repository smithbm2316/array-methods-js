doctype html
html

	head
		title Map - Array Methods
		link(rel='stylesheet' href="./css/main.css")
		link(rel="stylesheet" href="//cdnjs.cloudflare.com/ajax/libs/highlight.js/9.18.1/styles/default.min.css")
	body
		main
			.headline
				h1 Map
				p Use #[span.snippet map()] when you want to take an array of pre-existing data and manipulate that data, without affecting the original array.
				- 
					const names = [
						"Ben Smith",
						"John Michaelson",
						"Wes Tomkins",
						"Julie Donaldson",
						"Katelyn Roberts",
					];
					const firstNames = names.map(name => name.split(" ")[0]);
			
			.ex-one
				pre
					code.javascript
						p const names = [#{names.join(', ')}];
						p const firstNames = names.map(name => name.split(" ")[0]);
	
	script(src="//cdnjs.cloudflare.com/ajax/libs/highlight.js/9.18.1/highlight.min.js")
	script(src="./js/highlight-styles.js")