doctype html
html.bg-gray-200

	head
		title Map - Array Methods
		link(rel='stylesheet' href='https://unpkg.com/tailwindcss@^1.2.0/dist/tailwind.min.css')

	body
		main(class="w-5/6 mx-auto my-8 text-gray-900")
			.headline.mb-8
				h1.text-4xl.text-blue-700.font-bold.mb-2 Map
				p Use map() when you want to take an array of pre-existing data and manipulate that data, without affecting the original array.
			.codeblock.mb-8.shadow-inner.bg-gray-400.p-4.rounded-lg
				- 
					const names = [
						"Ben Smith",
						"John Michaelson",
						"Wes Tomkins",
						"Julie Donaldson",
						"Katelyn Roberts",
					];
					const firstNames = names.map(name => name.split(" ")[0]);
				
				span const names = [
				each name in names
					span.block.ml-4 "#{name}", 
				span ];

